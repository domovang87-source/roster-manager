import LandingMarketing from "../../components/LandingMarketing";

/** Public teaser — same as `/` but not redirected to `/home` when you’re signed in. */
export default function WelcomePage() {
  return <LandingMarketing />;
}
